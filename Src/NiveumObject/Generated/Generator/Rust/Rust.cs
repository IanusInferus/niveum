//==========================================================================
//
//  Notice:      This file is automatically generated.
//               Please don't modify this file.
//
//==========================================================================

#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using Boolean = System.Boolean;
using String = System.String;
using Type = System.Type;
using Int = System.Int32;
using Real = System.Double;
using Byte = System.Byte;
using UInt8 = System.Byte;
using UInt16 = System.UInt16;
using UInt32 = System.UInt32;
using UInt64 = System.UInt64;
using Int8 = System.SByte;
using Int16 = System.Int16;
using Int32 = System.Int32;
using Int64 = System.Int64;
using Float32 = System.Single;
using Float64 = System.Double;

namespace Niveum.ObjectSchema.Rust
{
    partial class Templates
    {
        public readonly List<String> Keywords = new List<String> {"_", "as", "async", "await", "break", "const", "continue", "crate", "dyn", "else", "enum", "extern", "false", "fn", "for", "if", "impl", "in", "let", "loop", "match", "mod", "move", "mut", "pub", "ref", "return", "self", "Self", "static", "struct", "super", "trait", "true", "type", "unsafe", "use", "where", "while", "abstract", "become", "box", "do", "final", "gen", "macro", "override", "priv", "try", "typeof", "unsized", "virtual", "yield"};
        public readonly Dictionary<String, String> PrimitiveMapping = new Dictionary<String, String> {{"Unit", "()"}, {"Boolean", "bool"}, {"String", "String"}, {"Int", "i32"}, {"Real", "f64"}, {"Byte", "u8"}, {"UInt8", "u8"}, {"UInt16", "u16"}, {"UInt32", "u32"}, {"UInt64", "u64"}, {"Int8", "i8"}, {"Int16", "i16"}, {"Int32", "i32"}, {"Int64", "i64"}, {"Float32", "f32"}, {"Float64", "f64"}, {"Type", "String"}, {"Optional", "Option"}, {"List", "Vec"}, {"Set", "HashSet"}, {"Map", "HashMap"}};
        private IEnumerable<String> Begin()
        {
            yield return "";
        }
        private IEnumerable<String> Combine(IEnumerable<String> Left, String Right)
        {
            foreach (var vLeft in Left)
            {
                yield return vLeft + Right;
            }
        }
        private IEnumerable<String> Combine(IEnumerable<String> Left, Object Right)
        {
            foreach (var vLeft in Left)
            {
                yield return vLeft + Convert.ToString(Right, System.Globalization.CultureInfo.InvariantCulture);
            }
        }
        private IEnumerable<String> Combine(IEnumerable<String> Left, IEnumerable<String> Right)
        {
            foreach (var vLeft in Left)
            {
                foreach (var vRight in Right)
                {
                    yield return vLeft + vRight;
                }
            }
        }
        private IEnumerable<String> Combine<T>(IEnumerable<String> Left, IEnumerable<T> Right)
        {
            foreach (var vLeft in Left)
            {
                foreach (var vRight in Right)
                {
                    yield return vLeft + Convert.ToString(vRight, System.Globalization.CultureInfo.InvariantCulture);
                }
            }
        }
        private IEnumerable<String> GetEscapedIdentifier(IEnumerable<String> IdentifierValues)
        {
            foreach (var Identifier in IdentifierValues)
            {
                yield return GetEscapedIdentifier(Identifier);
            }
        }
        public IEnumerable<String> Alias(AliasDef a)
        {
            var Name = GetEscapedIdentifier(a.DefinitionName());
            var GenericParams = GetGenericParameters(a.GenericParameters);
            var TypeParamLine = GetGenericParameterLine(a.GenericParameters);
            var Type = GetTypeString(a.Type, a.NamespaceName());
            foreach (var _Line in Combine(Begin(), GetDocComment(a.Description)))
            {
                yield return _Line;
            }
            yield return "/* Alias */";
            yield return "#[derive(Debug, Clone, PartialEq)]";
            foreach (var _Line in Combine(Begin(), TypeParamLine))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Combine(Combine(Combine(Combine(Combine(Begin(), "pub struct "), GetEscapedIdentifier(a.DefinitionName())), GenericParams), "(pub "), Type), ");"))
            {
                yield return _Line;
            }
        }
        public IEnumerable<String> Record(RecordDef r)
        {
            var Name = GetEscapedIdentifier(r.DefinitionName());
            var GenericParams = GetGenericParameters(r.GenericParameters);
            var TypeParamLine = GetGenericParameterLine(r.GenericParameters);
            foreach (var _Line in Combine(Begin(), GetDocComment(r.Description)))
            {
                yield return _Line;
            }
            yield return "/* Record */";
            yield return "#[derive(Debug, Clone, PartialEq)]";
            foreach (var _Line in Combine(Begin(), TypeParamLine))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Combine(Combine(Combine(Begin(), "pub struct "), GetEscapedIdentifier(r.DefinitionName())), GenericParams), " {"))
            {
                yield return _Line;
            }
            foreach (var f in r.Fields)
            {
                foreach (var _Line in Combine(Begin(), GetDocComment(f.Description)))
                {
                    yield return _Line == "" ? "" : "    " + _Line;
                }
                foreach (var _Line in Combine(Combine(Combine(Combine(Combine(Begin(), "pub "), GetEscapedIdentifier(f.Name)), ": "), GetTypeString(f.Type, r.NamespaceName())), ","))
                {
                    yield return _Line == "" ? "" : "    " + _Line;
                }
            }
            yield return "}";
        }
        public IEnumerable<String> TaggedUnion(TaggedUnionDef tu)
        {
            var Name = GetEscapedIdentifier(tu.DefinitionName());
            var GenericParams = GetGenericParameters(tu.GenericParameters);
            var TypeParamLine = GetGenericParameterLine(tu.GenericParameters);
            foreach (var _Line in Combine(Begin(), GetDocComment(tu.Description)))
            {
                yield return _Line;
            }
            yield return "/* TaggedUnion */";
            yield return "#[derive(Debug, Clone, PartialEq)]";
            yield return "#[repr(i32)]";
            foreach (var _Line in Combine(Begin(), TypeParamLine))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Combine(Combine(Combine(Begin(), "pub enum "), GetEscapedIdentifier(tu.DefinitionName())), GenericParams), " {"))
            {
                yield return _Line;
            }
            {
                var k = 0;
                foreach (var a in tu.Alternatives)
                {
                    if (a.Type.OnTypeRef && a.Type.TypeRef.NameMatches("Unit"))
                    {
                        foreach (var _Line in Combine(Begin(), GetDocComment(a.Description)))
                        {
                            yield return _Line == "" ? "" : "    " + _Line;
                        }
                        foreach (var _Line in Combine(Combine(Combine(Combine(Begin(), GetEscapedIdentifier(a.Name)), " = "), k), ","))
                        {
                            yield return _Line == "" ? "" : "    " + _Line;
                        }
                    }
                    else
                    {
                        foreach (var _Line in Combine(Begin(), GetDocComment(a.Description)))
                        {
                            yield return _Line == "" ? "" : "    " + _Line;
                        }
                        foreach (var _Line in Combine(Combine(Combine(Combine(Combine(Combine(Begin(), GetEscapedIdentifier(a.Name)), "("), GetBoxedTypeString(a.Type, tu.NamespaceName(), tu.VersionedName())), ") = "), k), ","))
                        {
                            yield return _Line == "" ? "" : "    " + _Line;
                        }
                    }
                    k += 1;
                }
            }
            yield return "}";
        }
        public IEnumerable<String> Enum(EnumDef e)
        {
            var Name = GetEscapedIdentifier(e.DefinitionName());
            var UnderlyingType = GetEnumTypeString(e.UnderlyingType, e.NamespaceName());
            foreach (var _Line in Combine(Begin(), GetDocComment(e.Description)))
            {
                yield return _Line;
            }
            yield return "#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]";
            foreach (var _Line in Combine(Combine(Combine(Begin(), "#[repr("), UnderlyingType), ")]"))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Combine(Combine(Begin(), "pub enum "), GetEscapedIdentifier(e.DefinitionName())), " {"))
            {
                yield return _Line;
            }
            foreach (var l in e.Literals)
            {
                foreach (var _Line in Combine(Begin(), GetDocComment(l.Description)))
                {
                    yield return _Line == "" ? "" : "    " + _Line;
                }
                foreach (var _Line in Combine(Combine(Combine(Combine(Begin(), GetEscapedIdentifier(l.Name)), " = "), l.Value), ","))
                {
                    yield return _Line == "" ? "" : "    " + _Line;
                }
            }
            yield return "}";
        }
        public IEnumerable<String> WrapNamespacePart(String NamespacePart, IEnumerable<String> Contents)
        {
            foreach (var _Line in Combine(Combine(Combine(Begin(), "pub mod "), GetEscapedIdentifier(NamespacePart)), " {"))
            {
                yield return _Line;
            }
            yield return "    use std::collections::{HashMap, HashSet};";
            foreach (var _Line in Combine(Combine(Begin(), "    "), Contents))
            {
                yield return _Line;
            }
            yield return "}";
        }
        public IEnumerable<String> Main(Schema Schema, String NamespaceName)
        {
            yield return "//==========================================================================";
            yield return "//";
            yield return "//  Notice:      This file is automatically generated.";
            yield return "//               Please don't modify this file.";
            yield return "//";
            yield return "//==========================================================================";
            yield return "";
            yield return "#![allow(dead_code)]";
            yield return "#![allow(non_camel_case_types)]";
            yield return "#![allow(non_snake_case)]";
            yield return "#![allow(non_upper_case_globals)]";
            yield return "#![allow(unused_imports)]";
            yield return "";
            yield return "use std::collections::{HashMap, HashSet};";
            yield return "";
            foreach (var _Line in Combine(Begin(), GetTypes(Schema, NamespaceName)))
            {
                yield return _Line;
            }
            yield return "";
        }
    }
}
