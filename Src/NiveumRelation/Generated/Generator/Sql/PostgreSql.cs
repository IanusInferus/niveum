//==========================================================================
//
//  Notice:      This file is automatically generated.
//               Please don't modify this file.
//
//==========================================================================

#nullable enable
#pragma warning disable CS8618

using System;
using System.Collections.Generic;
using System.Linq;
using Boolean = System.Boolean;
using String = System.String;
using Type = System.Type;
using Int = System.Int32;
using Real = System.Double;
using Byte = System.Byte;
using UInt8 = System.Byte;
using UInt16 = System.UInt16;
using UInt32 = System.UInt32;
using UInt64 = System.UInt64;
using Int8 = System.SByte;
using Int16 = System.Int16;
using Int32 = System.Int32;
using Int64 = System.Int64;
using Float32 = System.Single;
using Float64 = System.Double;

namespace Niveum.RelationSchema.PostgreSql
{
    partial class Templates
    {
        private IEnumerable<String> Begin()
        {
            yield return "";
        }
        private IEnumerable<String> Combine(IEnumerable<String> Left, String Right)
        {
            foreach (var vLeft in Left)
            {
                yield return vLeft + Right;
            }
        }
        private IEnumerable<String> Combine(IEnumerable<String> Left, Object Right)
        {
            foreach (var vLeft in Left)
            {
                yield return vLeft + Convert.ToString(Right, System.Globalization.CultureInfo.InvariantCulture);
            }
        }
        private IEnumerable<String> Combine(IEnumerable<String> Left, IEnumerable<String> Right)
        {
            foreach (var vLeft in Left)
            {
                foreach (var vRight in Right)
                {
                    yield return vLeft + vRight;
                }
            }
        }
        private IEnumerable<String> Combine<T>(IEnumerable<String> Left, IEnumerable<T> Right)
        {
            foreach (var vLeft in Left)
            {
                foreach (var vRight in Right)
                {
                    yield return vLeft + Convert.ToString(vRight, System.Globalization.CultureInfo.InvariantCulture);
                }
            }
        }
        private IEnumerable<String> GetEscapedIdentifier(IEnumerable<String> IdentifierValues)
        {
            foreach (var Identifier in IdentifierValues)
            {
                yield return GetEscapedIdentifier(Identifier);
            }
        }
        public IEnumerable<String> Main(String DatabaseName, IEnumerable<String> Tables, IEnumerable<String> ForeignKeys, IEnumerable<String> Comments)
        {
            yield return "--==========================================================================";
            yield return "--";
            yield return "--  Notice:      This file is automatically generated.";
            yield return "--               Please don't modify this file.";
            yield return "--";
            yield return "--==========================================================================";
            yield return "";
            yield return "--USE postgres";
            yield return ";";
            yield return "";
            foreach (var _Line in Combine(Combine(Combine(Begin(), "DROP DATABASE IF EXISTS \""), DatabaseName), "\""))
            {
                yield return _Line;
            }
            yield return ";";
            yield return "";
            foreach (var _Line in Combine(Combine(Combine(Begin(), "CREATE DATABASE \""), DatabaseName), "\""))
            {
                yield return _Line;
            }
            yield return ";";
            yield return "";
            foreach (var _Line in Combine(Combine(Combine(Begin(), "--USE \""), DatabaseName), "\""))
            {
                yield return _Line;
            }
            yield return ";";
            yield return "";
            foreach (var _Line in Combine(Begin(), Tables))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Begin(), ForeignKeys))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Begin(), Comments))
            {
                yield return _Line;
            }
        }
        public IEnumerable<String> Table(String Name, IEnumerable<String> FieldsAndKeys, IEnumerable<String> NonUniqueKeys)
        {
            foreach (var _Line in Combine(Combine(Combine(Begin(), "CREATE TABLE \""), Name), "\""))
            {
                yield return _Line;
            }
            yield return "(";
            foreach (var _Line in Combine(Combine(Begin(), "    "), FieldsAndKeys))
            {
                yield return _Line;
            }
            yield return ")";
            yield return ";";
            yield return "";
            foreach (var _Line in Combine(Begin(), NonUniqueKeys))
            {
                yield return _Line;
            }
        }
        public IEnumerable<String> Key(String KeyKeyword, String Name, String ClusterKeyword, IEnumerable<String> Columns)
        {
            foreach (var _Line in Combine(Combine(Combine(Combine(Combine(Combine(Begin(), "CONSTRAINT \""), Name), "\" "), KeyKeyword), " --"), ClusterKeyword))
            {
                yield return _Line;
            }
            yield return "(";
            foreach (var _Line in Combine(Combine(Begin(), "    "), Columns))
            {
                yield return _Line;
            }
            yield return ")";
        }
        public IEnumerable<String> NonUniqueKey(String Name, String TableName, String ClusterKeyword, IEnumerable<String> Columns)
        {
            foreach (var _Line in Combine(Combine(Combine(Combine(Begin(), "CREATE INDEX \""), Name), "\" --"), ClusterKeyword))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Combine(Combine(Begin(), "ON \""), TableName), "\""))
            {
                yield return _Line;
            }
            yield return "(";
            foreach (var _Line in Combine(Combine(Begin(), "    "), Columns))
            {
                yield return _Line;
            }
            yield return ")";
            yield return ";";
            yield return "";
        }
        public IEnumerable<String> ForeignKey(String Name, String ThisTableName, IEnumerable<String> ThisKeyColumns, String OtherTableName, IEnumerable<String> OtherKeyColumns)
        {
            foreach (var _Line in Combine(Combine(Combine(Begin(), "ALTER TABLE \""), ThisTableName), "\""))
            {
                yield return _Line;
            }
            foreach (var _Line in Combine(Combine(Combine(Begin(), "ADD CONSTRAINT \""), Name), "\" FOREIGN KEY"))
            {
                yield return _Line;
            }
            yield return "(";
            foreach (var _Line in Combine(Combine(Begin(), "    "), ThisKeyColumns))
            {
                yield return _Line;
            }
            yield return ")";
            foreach (var _Line in Combine(Combine(Combine(Begin(), "REFERENCES \""), OtherTableName), "\""))
            {
                yield return _Line;
            }
            yield return "(";
            foreach (var _Line in Combine(Combine(Begin(), "    "), OtherKeyColumns))
            {
                yield return _Line;
            }
            yield return ")";
            yield return ";";
            yield return "";
        }
        public IEnumerable<String> TableComment(String Name, String Description)
        {
            foreach (var _Line in Combine(Combine(Combine(Combine(Begin(), "COMMENT ON TABLE \""), Name), "\" IS "), Description))
            {
                yield return _Line;
            }
            yield return ";";
            yield return "";
        }
        public IEnumerable<String> ColumnComment(String TableName, String ColumnName, String Description)
        {
            foreach (var _Line in Combine(Combine(Combine(Combine(Combine(Combine(Begin(), "COMMENT ON COLUMN \""), TableName), "\".\""), ColumnName), "\" IS "), Description))
            {
                yield return _Line;
            }
            yield return ";";
            yield return "";
        }
    }
}
